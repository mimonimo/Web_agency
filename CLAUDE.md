# CLAUDE.md — 누적 기록

> BRIEF 작업규칙 6: Phase 종료 시 이번에 내린 결정과 남은 이슈를 여기에 누적 기록한다.
> 새 세션은 이 파일을 먼저 읽는다.

---

# ★ 새 PC 에서 이어받는다면 — 여기부터

```bash
git clone https://github.com/mimonimo/Web_agency.git agora && cd agora
./ops/bootstrap.sh        # venv · 의존성 · .env · 작업 디렉터리 (멱등)
./ops/dev.sh start        # → http://localhost:8000
```

`bootstrap.sh` 는 **노드가 없는 PC** 를 전제로 `EXECUTOR=sim` 을 잡는다.
그 상태로 주문 접수부터 배포까지 전 구간이 몇 분 만에 돈다 — 흐름과 화면을
전부 확인할 수 있다. 실제 DGX 노드가 있으면 `.env` 에서 `EXECUTOR=a2a` 로 바꾼다.

## 클론에 따라오지 않는 것 (`.gitignore`)

| | |
|---|---|
| `.env` | `bootstrap.sh` 가 `.env.example` 에서 만든다 |
| `repo/agora.db` | 첫 기동 때 자동 생성 |
| `repo/project-001/` `repo/runs/` | 첫 사이클을 돌리면 생긴다 |
| `.venv/` | `bootstrap.sh` 가 만든다 |

**`repo/showcase/` 는 따라온다.** 지난 결과물 3벌(로컬 개선 전/후 · Claude 레퍼런스)이
들어 있어 클론 직후에도 `/projects.html` 에서 바로 볼 수 있다.

## 먼저 읽을 것

| 파일 | 무엇 |
|---|---|
| [`README.md`](README.md) | 30초 요약 · 설계 결정 3가지 · 결과 |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | 구조 · 상태기계 · 지시 조립 순서 · 고장 대비 |
| [`FLOW.md`](FLOW.md) | 수업 흐름 · 사람이 개입하는 5지점 |
| [`docs/KNOWN-ISSUES.md`](docs/KNOWN-ISSUES.md) | **실제로 나온 결함 14건** + 아직 못 막은 것 |
| [`agents/`](agents/) | 11개 역할 기준선 지시문 (사람이 쓴다) |
| [`core/app/roles.yaml`](core/app/roles.yaml) | 역할 목표·완료조건 (코드 아님, 데이터) |

## 손대기 전에 알아야 할 것 세 가지

1. **품질은 모델이 아니라 구조가 잡는다.** 지시문에 "하지 마라" 를 적는 것만으로는
   안 막힌다 — 실측했다. 막히는 것은 `checks.py` 의 **검사 → 실패목록 → 재실행** 루프다.
   새 결함을 발견하면 `docs/KNOWN-ISSUES.md` 에 적고, `checks.py` 에 검사를 넣고,
   `core/tests/test_roles.py` 에 그 검사의 테스트를 넣는다.
2. **`reset` 의 기본값은 `keep_specs=True`.** 학생이 고친 AGENT.md 를 리셋 한 번에
   날리면 수업이 망한다. 이 기본값을 바꾸지 마라.
3. **`repo/runs/` 를 손으로 지우지 마라.** 완성된 사이트가 거기 있다. 초기화는
   `ops/reset.sh` 로 — `repo/.archive/` 에 백업을 남긴다.
   (실제로 한 번 날렸다가 노드에서 51개를 회수했다.)

## 확인 명령

```bash
make test                      # 상태기계 38 + 역할카탈로그 95 + E2E
./ops/acceptance.sh            # 전체 인수 (HQ 가 떠 있어야 한다)
./ops/reference-check.sh --all # 결과물 채점 · 세 판 비교
.venv/bin/python ops/site-check.py <사이트폴더>   # 19항목
```

## ops-node/ 를 실제로 쓰려면

공개 리포라 **노드 IP 를 `10.0.0.x` 예시 값으로 바꿔** 올렸고,
노드 sudo 비밀번호는 `NODE_SUDO_PW` 환경변수로 받게 고쳤다.
`ops-node/nodes.tsv` 를 자기 환경 값으로 바꾸면 나머지 스크립트가 전부 거기서 읽는다.
자세한 것은 [`ops-node/SECURITY-NOTE.md`](ops-node/SECURITY-NOTE.md).

---

## 진행 상황

| Phase | 범위 | 인수 | 상태 |
|---|---|---|---|
| 노드 준비 | dgx-02~11 Hermes + Ollama | — | ✅ 2026-07-28 |
| **0** | 리포 골격 · CLAUDE.md · Makefile · compose 스켈레톤 | 구조 일치 | ✅ 2026-07-28 |
| 1 | DB · 마이그레이션 (caddy·dnsmasq 는 보류) | 4 | 🟡 부분 |
| **2** | **노드 A2A 어댑터 · 미러링 · 토큰** | 5–10 | ✅ 2026-07-28 |
| **3** | **오케스트레이터 · 스펙 생명주기 · 반려/되감기/리셋** | 11–25 | ✅ 2026-07-28 |
| **4** | **PM 관제 화면 · 주문 사이트 · 학생 편집기** | 26–32 | ✅ 2026-07-28 |
| **5** | **부트스트랩 · verify · 전체 리셋 · 자동 기동** | 33–36 | ✅ 2026-07-28 |

---

## 사전 작업 — 노드 준비 (2026-07-28)

BRIEF 를 받기 전 상태와 실제가 달라서 먼저 정리했다.

| 항목 | 착수 전 실제 |
|---|---|
| Ollama + `gpt-oss:120b` | 10대 전부 설치·기동 (정상) |
| Hermes 설치됨 | dgx-05, 06, 07, 08, 09 — **5대뿐** |
| Hermes 미설치 | dgx-02, 03, 04, 10, 11 |
| Hermes → Ollama 연결 | **0대**. 전부 `openrouter.ai` + `anthropic/claude-opus-4.6` |

Hermes 가 있던 5대조차 **외부 유료 API** 를 바라보고 있었다. BRIEF §1.2 의
"인터넷 없음 전제"와 정면으로 어긋나므로 전량 로컬 Ollama 로 돌렸다.

### 내린 결정

1. **PM 은 SSH 공개키로 노드를 관리한다.** `~/.ssh/id_ed25519` (comment `dgx-12-pm`) 를
   10대 `authorized_keys` 에 배포. 비밀번호(설치 시 정한 값)는 **삭제하지 않았다** —
   키가 깨져도 교실에서 잠기지 않게.
2. **모델은 노드 로컬 Ollama.** 중앙 1대에 모아 서빙하지 않는다.
   `provider: ollama` / `base_url: http://127.0.0.1:11434/v1` / `model: gpt-oss:120b`.
   네트워크를 안 타므로 인터넷·L2 장애에 영향받지 않는다.
   Ollama 는 `127.0.0.1` 만 듣게 **그대로 뒀다** (무인증이라 외부 노출은 위험).
3. **설정은 `hermes config set` 으로만 건드린다.** `config.yaml` 직접 편집 금지.
   기존 설정은 노드별 `~/.hermes/config.yaml.pre-ollama.bak` 에 백업.
4. **역할 매핑은 노드 번호 순서** (PM 승인). `~/agora-ops/nodes.tsv` 가 단일 진실 공급원.
5. **`agents/<role>/AGENT.md` 는 만들지 않았다.** BRIEF §10 · §4.1 — 초안 생성은
   S2 기획 에이전트의 몫이고 그게 수업 내용 그 자체다.

### 검증한 것

10대 전부 로컬 Ollama 를 통한 **실제 추론 왕복 성공**. 프로비저닝 2회차 재실행으로
멱등성 확인 (10/10 PASS, 변화 없음).

PM 운영 도구는 `~/agora-ops/` — 이 리포와 별개다 (노드 운영용이지 HQ 가 아니므로).

---

## Phase 0 — 리포 골격 (2026-07-28)

### 내린 결정

1. **HQ 는 dgx-12 (PM PC) 위에 올린다.** BRIEF §1.2 는 "별도 VM(Ubuntu)" 을 말하지만
   가용 장비가 DGX 11대뿐이다 (PM 승인). 디스크 3.4T 여유, 80/8000 포트 비어 있음.
2. **런타임은 Docker Compose** — BRIEF §1.2 대로. 네이티브 대안도 검토했으나
   BRIEF 구성을 지키는 쪽을 택했다 (PM 승인).
3. **`pm` 에이전트는 dgx-12 가 겸임** → 등록 노드 총 **11개**.
   BRIEF 인수 #5("노드 11개 register")를 구조 변경 없이 만족한다 (PM 승인).
4. **`models.py` 는 Phase 0 에서 컬럼까지 확정했다.** 골격만 두는 것이 원칙이지만,
   BRIEF §6 의 컬럼명이 이후 전 세션의 계약이라 여기서 못 박는 편이 안전하다.
   마이그레이션 실행은 Phase 1.
5. **라우터는 501 을 던지되 시그니처는 진짜로 선언했다.** `/docs` 를 열면 전체 API
   표면이 바로 드러나고, 이후 세션은 빈칸만 채운다. 어느 Phase 에서 구현하는지를
   에러 메시지에 한국어로 적었다 (BRIEF §15-4).
6. **`orchestrator.py` 는 순수 함수 계약만 뒀다** (BRIEF §15-1).
   `next_step(cycle, steps, event) -> Transition`. Session 을 인자로 받지 않고
   import 하지도 않는다. DB 반영은 `routers/cycles.py` 의 몫.
7. **`pipeline.yaml` 4종은 스텁이 아니라 실물로 작성했다.** 파이프라인은 코드가 아니라
   데이터이고 (BRIEF §3.6) BRIEF 에 전문이 주어져 있으므로 미룰 이유가 없다.
8. **`make preload` 를 만들었다.** 교실에는 인터넷이 없다 (BRIEF 작업규칙 7).
   인터넷이 되는 지금 이미지 pull 과 core 빌드를 끝내 두기 위한 타깃이다.
9. **`dnsmasq` 는 compose 에 넣지 않고 Phase 1 로 미뤘다.** 호스트의
   `systemd-resolved` 가 이미 53 번을 잡고 있어 포트 처리 방식을 먼저 정해야 한다.
10. **`.env` 와 `provisioning/out/` 은 gitignore.** 토큰·비밀 커밋 금지 (BRIEF §15-6).
    `repo/runs/` 도 제외했다 — 재생성 가능하고 reset 으로 날아가는 것들이다.

### 남은 이슈

- **★ Docker 그룹 권한이 없다.** dgx-12 계정이 `docker` 그룹에 없어 컨테이너를 띄울 수 없다.
  **Phase 1 `make up` 의 선행 조건**이다:
  ```
  sudo usermod -aG docker $USER     # 후 재로그인
  ```
  Phase 0 는 골격만 만들므로 영향 없이 끝났다.

- **★ BRIEF 내부 모순 — defect 파이프라인의 시작 지점.**
  §3.6 본문은 "셋 다 **S1 영업에서 시작**하는 것은 동일하다" 라고 하는데,
  인수 #24 는 "pipeline.defect.yaml 로 Cycle 생성 → **S6(QA)부터 시작**하는지 확인"
  이라고 한다. 둘이 어긋난다.
  현재 `pipeline.defect.yaml` 은 본문을 따라 S1 을 접수 전용(`intake_only`)으로 두었다.
  **Phase 3c 착수 전 PM 확인 필요.**

- **A2A 표준 SDK vs 자체 JSON-RPC 미결정** (BRIEF §5.4).
  Phase 2 의 첫 작업으로 오프라인 설치 가능 여부를 확인한 뒤 정하고,
  **어느 쪽을 택했는지 여기에 반드시 기록한다.**

- **학생 명단이 없다.** `provisioning/students.yaml` 의 `name` 은 전부 비어 있다.
  역할·노드·IP 는 채워져 있으므로 이름만 넣고 `make provision` 을 다시 돌리면 된다.

- **`repo/` 의 git 초기화를 아직 안 했다.** bare git + 작업 트리 구성은 Phase 1.

- **계약 씨앗 4종이 아직 없다** (BRIEF §11 — `SRS.md`, `design-tokens.json`,
  `schema.sql`, `api-contract.yaml`). `provision.py` 가 만드는 것이라 Phase 2.
  **빈 파일 금지** — 학생이 백지에서 시작하면 하루가 무너진다.

- **노드에 A2A 어댑터가 없다.** 41241 포트는 아직 아무것도 안 듣는다. Phase 2.

- **Hermes 상주(데몬) 미설정.** 현재는 호출할 때만 뜨는 CLI 상태다.
  BRIEF §1.2 의 "24시간 켜둠" 은 A2A 어댑터가 생긴 뒤 systemd 유닛으로 잡는 게 맞다.

- **노드 `~/.hermes/.env` 에 OpenRouter 등 외부 키가 남아 있다.** 로컬 모델만 쓰므로
  당장 무해하지만, 학생에게 노드를 넘기기 전 정리할지 PM 판단 필요.


---

## Phase 1~4 — 오케스트레이터와 PM 화면 (2026-07-28)

PM 지시로 우선순위를 바꿨다: **"전체 로직과 흐름 성공, PM 작업 화면을 중점적으로.
IP 는 통신만 되면 되니 생 IP 그대로. 복잡하게 안 해도 됨."**

### 방향 전환 — 내린 결정

1. **Docker 를 기다리지 않고 네이티브로 먼저 돌렸다.**
   dgx-12 계정이 `docker` 그룹에 없어 `make up` 이 막혀 있었다. 권한을 기다리는 대신
   venv + uvicorn + SQLite 로 도는 경로(`make dev`)를 만들었다.
   `docker-compose.yml` 은 그대로 두었고, 권한이 생기면 `make up` 으로 옮겨가면 된다.
   **코드와 산출물 경로는 양쪽이 완전히 동일하다.**

2. **DNS(dnsmasq)를 버렸다.** PM 지시대로 생 IP 를 쓴다.
   `hq.agora.lan` → `10.0.0.62`. BRIEF 인수 #2·#3 은 이에 따라 보류.

3. **DB 는 SQLite 를 기본으로.** Postgres 용 Alembic 마이그레이션은 만들어 두었고
   Postgres 방언으로 SQL 이 정상 렌더되는 것까지 확인했다 (인수 #4 의 코드 부분).
   컨테이너로 옮기면 `DATABASE_URL` 만 바꾸면 된다.
   - SQLite 는 `BIGINT` PK 를 자동증가시키지 않아 `BigInteger().with_variant(Integer, "sqlite")`
     로 처리했다. 타임존도 보존하지 않아 `services.as_utc()` 로 정규화한다.

4. **실행 백엔드를 갈아끼울 수 있게 했다** (`EXECUTOR` 환경변수).
   - `sim` — HQ 안에서 산출물을 흉내낸다. **노드 없이 흐름과 화면을 확인**하는 용도.
   - `a2a` — 실제 학생 노드의 Hermes 를 호출한다 (Phase 5 에서 연결).
   어느 쪽이든 상태기계와 산출물 경로는 동일하다. 수업 리허설은 sim, 당일은 a2a.

5. **노드 하트비트를 먼저 붙였다.** A2A 어댑터는 아직 없지만, 노드가 살아 있다는 것만은
   PM 화면에 진짜로 떠야 한다. 30초 주기 `curl` 루프 + `@reboot` crontab
   (sudo 불필요). 11대 전부 등록·생존 확인 — 인수 #5·#7 만족.

6. **`_FLAGS` 는 메모리에 둔다.** graceful pause 예약과 single-step 플래그는
   Cycle 테이블에 컬럼을 더하지 않고 프로세스 메모리에 뒀다. HQ 가 한 프로세스뿐인
   교실 환경이라 이걸로 충분하다. **HQ 를 재시작하면 이 두 플래그는 날아간다** —
   여러 프로세스로 늘릴 일이 생기면 컬럼으로 옮겨야 한다.

### 잡은 버그 (E2E 테스트가 잡아냈다)

- **사람 게이트를 통과해도 그 단계가 PENDING 으로 남았다.** 게이트 진입 시
  `WAITING_HUMAN`, 통과 시 `DONE` 으로 닫도록 `wait_step`/`complete_step` 을 추가.
- **`reset(keep_specs=false)` 가 파일은 초안으로 되돌리면서 해시를 갱신하지 않았다.**
  다음 폴링이 "학생이 고쳤다" 로 오인해 곧바로 customized 로 되돌아갔다.
  초안 기준으로 해시를 다시 계산하도록 수정.
- `create_cycle` 이 `mode` 를 문자열로 넣어 `.value` 접근이 터졌다 → Enum 으로 강제.

### 검증한 것 (전부 실행 출력 있음)

```
ops/acceptance.sh          통과 61 · 실패 0
  Phase 0 구조             46건
  Phase 3 오케스트레이터    상태기계 38건 + E2E 흐름 46건
  Phase 4 PM 화면          12건
```

`ops/demo.sh` 로 수업 시나리오 전체가 실제로 돈다:
주문 → S2 가 AGENT.md 11개 생성 → S3 자동 정지 → 학생이 고치면 카운터가 오름 →
11/11 에서 자동 재개 → QA 반려 → 되감기 → 재작업 → 완주.

### 남은 이슈

- **★ 노드 A2A 어댑터가 없다.** 지금은 `EXECUTOR=sim` 으로 HQ 가 산출물을 흉내낸다.
  **학생 노드의 Hermes 가 실제로 일하게 하려면** `node/a2a-adapter/` 를 만들고
  `runner._execute_a2a()` 를 연결해야 한다. 이게 다음 순서의 핵심이다.
  - 표준 A2A SDK vs 자체 JSON-RPC 는 **아직 미결정** (BRIEF §5.4).
- **Docker 그룹 권한** — `sudo usermod -aG docker $USER` 후 재로그인.
  하면 `make up` 으로 Postgres·caddy 구성으로 옮길 수 있다. 지금은 없어도 돌아간다.
- **board.html 은 아직 자리만 잡아 뒀다.** 티켓 보드는 PM 화면 우측에 요약만 있다.
- **인수 #8~#10** (노드 왕복, 노드↔노드 미러링, 잘못된 토큰 401) — 어댑터 이후.
- **인수 #33~#36** (부트스트랩 멱등, verify-node, 전체 리셋, 재부팅 자동기동).
- **defect 파이프라인 S1/S6 모순** — 여전히 미해결. 위 Phase 0 기록 참조.
- **학생 명단 없음** — `provisioning/students.yaml` 의 `name` 이 전부 비어 있다.


---

## Phase 2·5 — 실제 노드 연결과 운영 (2026-07-28)

### ★ BRIEF §5.4 에 대한 답 — 표준 SDK 를 쓸 것인가

**확인 결과: `a2a-sdk 1.1.2` 는 오프라인 설치가 가능하다.** 의존성 30개까지
aarch64 wheel 이 전부 존재하고 `ops/wheels/` 에 미리 받아 두었다.

**그런데도 쓰지 않았다.** 와이어 포맷(JSON-RPC 2.0 · `message/send` · `tasks/get` ·
`/.well-known/agent-card.json`)은 A2A 와 **글자 그대로 동일**하게 맞추고 구현만 직접 했다.

- 이유: 노드 어댑터가 하는 일은 네 가지뿐인데(BRIEF §5.5), SDK 를 쓰면 학생 노드 11대에
  google-api-core · protobuf · cryptography 를 포함한 31개 패키지와 venv 를 얹어야 한다.
  `python3 adapter.py` 한 줄로 뜨는 쪽이 교실에서 훨씬 안전하다.
- 필드명·엔드포인트가 같으므로 **나중에 SDK 로 갈아끼울 수 있다.**
  오프라인 설치 명령은 `ops/wheels/README.md` 에 있다.

### 실제로 돌려서 잡은 결함 (전부 sim 에서는 안 보이던 것들)

1. **영업(S1)이 주문서를 못 받아 고객사를 지어냈다.** "클라이언트 XYZ", React/AWS 등.
   `inputs: [order]` 가 프롬프트에 실리지 않았다 → 주문 원문을 `order` 필드로 전달.
2. **기획(S2)이 AGENT.md 11개 중 2개만 쓰고 멈췄다.** 한 번의 호출로 11개는 모델에 무리다
   → **역할당 1회씩** 나눠 요청하도록 변경.
3. **11개가 전부 같은 내용이 됐다.** 11번의 요청이 같은 작업 디렉터리를 공유해
   앞 역할이 만든 파일을 다시 집어왔다. `endswith("AGENT.md")` 매칭도 원인이었다
   → 역할별 `work_key` 로 디렉터리 분리 + 정확한 경로 매칭.
4. **품질 게이트를 넣었다.** `runner.spec_ok()` — 6칸이 `## 제목`으로 나뉘고 최소 분량이
   있어야 통과. 미달이면 그 역할만 다시 시킨다. 한 줄로 압축된 초안은 학생이 고칠 자리가 없다.

### ★ 학생이 AGENT.md 를 고치는 경로 — 없던 것을 만들었다

이게 수업의 핵심인데 **경로가 아예 없었다.** 파일은 HQ(dgx-12) 한 곳에만 있고
학생들은 dgx-02~11 에 앉아 있어서, dgx-12 에 로그인한 PM 말고는 고칠 수가 없었다.

- `PUT /api/specs/{role}/raw` 를 만들었다. front-matter 는 HQ 가 관리하므로
  학생이 지우거나 고쳐도 원래 것으로 되돌린다.
- `web/edit.html` — 역할 고르기 · 본문 편집 · 저장. 저장하면 곧바로 스캔이 돌아
  카운터가 오르고, 11명이 다 채우면 사이클이 자동 재개된다.
- git 클론/푸시(BRIEF §4.2 원안)는 쓰지 않았다. 고등학생 11명이 30분 안에 동시에
  고쳐야 하는 상황에서 브라우저 편집이 압도적으로 빠르고 사고가 적다.
  대신 diff 는 그대로 보관하므로 회고용 평가 데이터는 잃지 않는다.

### 그 밖에 넣은 것

- **노드 토큰** (`core/app/auth.py`, 인수 #10). `NODE_TOKEN_SECRET` 에서 역할별로
  HMAC 파생 — 별도 저장소가 필요 없고 언제든 같은 값을 다시 만든다.
  틀린 토큰이면 401, 남의 역할 사칭 미러링도 401.
  `REQUIRE_NODE_TOKEN=0` 이 기본 — 수업 첫날 노드가 토큰을 못 받아 전부 401 이 나면 곤란하다.
- **`ops/reset.sh`** (인수 #35). 지우기 전에 `repo/.archive/<타임스탬프>/` 로 백업한다.
  학생 작업물을 실수로 날리면 되돌릴 수 없기 때문이다.
- **`ops/autostart.sh`** (인수 #36). crontab `@reboot` — sudo 불필요.
  노드 어댑터도 같은 방식으로 등록돼 있다.
- **dgx-12 가 pm 노드를 겸임한다.** `~/agora-node/` 에 어댑터를 따로 뒀다
  (`~/agora` 는 HQ 리포라 겹친다). `AGORA_NODE_DIR` 로 경로를 바꿀 수 있게 했다.

### 검증 (전부 실행 출력 있음)

```
ops/acceptance.sh              통과 82 · 실패 0
  Phase 0 구조                 46건
  Phase 3 오케스트레이터        상태기계 38 + E2E 흐름 46 + 학생 편집 16
  Phase 2 노드·A2A             7건 (카드 11/11, 하트비트 11/11, 토큰 401)
  Phase 4 PM 화면              15건
  Phase 5 부트스트랩·운영       9건
core/tests/test_a2a.py         11건 — 실제 Hermes 왕복 30~55초
```

`ops/reset.sh` 실행 결과: 산출물 62건·AGENT.md 11개 → 0, 백업 보존, 재기동 시 노드 11개 복구.

### 남은 이슈

- **E2E·편집 테스트는 `EXECUTOR=sim` 에서만 돈다.** 실제 노드는 한 단계에 수십 초가 걸려
  테스트 대기시간을 넘긴다. `acceptance.sh` 가 a2a 모드면 건너뛰고 그 사실을 출력한다.
  **수업 당일에는 `.env` 를 `EXECUTOR=a2a` 로 두어야 한다.**
- **실제 노드로 사이클 완주는 아직 못 봤다.** S1→S2→S3(게이트)까지는 확인했다.
  S4 이후 전 구간을 a2a 로 완주시키면 20~40분쯤 걸릴 것으로 보인다. 리허설이 필요하다.
- **dgx-12 의 8000·11434 가 ufw 로 막혀 있다.** `sudo bash ~/agora-ops/expose-dgx12.sh`
  (root 필요). 노드 10대와 dgx-01 의 11434 는 열어 두었다.
- **Docker 그룹 권한** — 여전히 없다. 네이티브(`make dev`)로 돌아가므로 급하지 않다.
- **게이트 판정이 아직 자동이 아니다.** QA·보안·검수 에이전트가 `VERDICT.md` 를 쓰지만
  HQ 가 그걸 읽어 통과/반려를 자동 결정하지는 않는다. 지금은 강사가 반려 버튼을 누른다.
- **board.html 은 자리만 잡아 뒀다.**
- **defect 파이프라인 S1/S6 모순** — 미해결. Phase 0 기록 참조.
- **학생 명단 없음** — `provisioning/students.yaml` 의 `name` 이 전부 비어 있다.


---

## 리허설과 웹 기능 보강 (2026-07-28)

### ★ 실제 노드로 전 구간 완주 — 24분

`ops/rehearsal.sh` 로 `EXECUTOR=a2a` 전 구간을 돌렸다. **10/10 단계 DONE.**

```
[  0분] 주문 접수 → S1 영업
[  1분] S2 기획 (AGENT.md 11개)
[ 10분] S3 게이트 → 학생 11명 저장 → ★자동재개
[ 10분] S4 설계 (4개 노드 병렬, 3분)
[ 13분] S5 구현 · [18분] S6 QA · [20분] S7 보안
[ 21분] S8 검수 · [22분] S9 배포 · [23분] S10 운영
[ 24분] DONE 10/10
```

**프론트엔드 에이전트가 실제로 도는 웹페이지를 만들었다** — 「밀밭제과 예약 주문」
`index.html` + `style.css` + `app.js`. 주문서 내용이 그대로 반영돼 있다.

수업 운영 기준: **한 사이클 25~30분**. 게이트 대기 시간까지 넣으면 한 차시에 한 바퀴다.

### 잡은 문제

- **`npm install` 로 `node_modules` 663개 파일이 산출물로 잡혔다.**
  어댑터 `collect()` 에 제외 목록(`node_modules`, `.git`, `dist`, `__pycache__` 등)과
  최대 60개 상한을 걸었다. → 6개로 줄었다.
  ⚠️ 노드에 인터넷이 있어서 `npm install` 이 됐다. **교실에서는 안 된다.**
  프론트엔드 AGENT.md 의 '금지' 칸에 "외부 패키지를 설치하지 마라" 를 넣어야 한다.
- **편집 페이지가 본문을 잘라서 보여줬다.** JS `split("---", 3)` 은 파이썬의 maxsplit 과
  달라서 본문에 `---` 구분선이 있으면 그 뒤가 날아간다. 정규식으로 바꿨다.
- **리허설 스크립트가 저장된 AGENT.md 본문을 지웠다.** `sed '1,/^---$/d'` 두 번이
  front-matter 뿐 아니라 본문까지 삭제했다. 파이썬 정규식으로 교체.
- **게이트 단계가 지시 템플릿을 못 찾았다.** `task` 가 비어 있어 `None.md` 를 찾고 있었다.
- **완료 메시지가 두 번씩 미러링됐다.** 노드와 HQ 가 같은 문장을 넣고 있었다.

### 웹에서 할 수 있게 만든 것

BRIEF 원안은 픽셀 오피스 하나였지만, **작업을 웹에서 끝낼 수 있어야** 교실에서 쓸 수 있다.

| 화면 | 하는 일 |
|---|---|
| `/index.html` | 픽셀 오피스. **책상을 누르면 에이전트 상세로** |
| `/agent.html` | 역할별 상세 — 현재 작업, 맡은 단계, 만든 파일(열어서 수정), 메시지, **↻재요청** |
| `/files.html` | `repo/` 탐색기 — 폴더 이동, 파일 조회·수정, 사이트 **열기** |
| `/board.html` | 티켓 4열 보드 + 단계 타임라인 |
| `/edit.html` | 학생용 AGENT.md 편집 — 경로 표시·복사, 편집/원문/초안비교 3탭 |
| `/preview/...` | **에이전트가 만든 웹사이트를 그대로 열어 본다** |

새 API: `GET/PUT /api/files`, `GET /api/files/tree`, `GET /api/agents/{role}`,
`PUT /api/agents/{role}/file`, `POST /api/agents/{role}/retry`,
`GET /preview/sites`, `GET /preview/{path}`.

- **재요청(retry)은 되감기(rewind)와 다르다.** 되감기는 그 이후 전부 무효화하지만
  재요청은 **그 단계 하나만** 다시 돌린다. 산출물이 마음에 안 들 때 쓴다.
  사이클이 RUNNING 이면 받지 않는다 — 먼저 일시정지해야 한다.
- 파일 읽기·쓰기는 전부 `repo/` 밖으로 못 나간다. 경로 탈출을 막았다.

### 게이트 자동 판정 (인수 #19 보강)

QA·보안·검수 에이전트가 쓴 `VERDICT.md` 의 첫 줄(`판정: 통과|반려`)을 HQ 가 읽어
자동으로 반려·되감기까지 한다. `GATE_AUTO=0` 으로 끄면 강사가 수동으로 누른다.
**판정을 못 읽으면 통과로 본다** — 게이트가 판정을 못 했다고 사이클을 멈춰 세우면
수업이 진행되지 않는다.

### 검증

```
ops/acceptance.sh   통과 83 · 실패 0
ops/rehearsal.sh    실제 노드 10/10 단계 DONE (24분)
```


---

## 산출물 복구와 사이트 미리보기 (2026-07-28)

### 사고와 복구

인수 테스트를 돌리려고 `rm -rf repo/runs` 를 했는데, **거기 리허설 결과물이 들어 있었다.**
프론트엔드가 만든 「밀밭제과 예약 주문」 사이트가 통째로 날아갔다.

**노드에는 원본이 남아 있었다.** 어댑터는 `~/agora/workspace/cycle-<id>/<step>/output/`
에 쓰고 그것을 HQ 로 올려보낼 뿐, 자기 사본은 지우지 않는다.
`~/agora-ops/recover-artifacts.sh <cycle>` 로 10대에서 51개를 전부 회수했다.

복구 스크립트를 만들면서 겪은 것 — **`while read` 루프 안의 `ssh` 는 `-n` 이 필요하다.**
안 그러면 ssh 가 루프의 stdin(`nodes.tsv`)을 통째로 먹어서 첫 노드 뒤로 전부 건너뛴다.
목록은 별도 fd(`3<`)로 읽게 했다.

### 다시 안 나게 한 것

- `ops/acceptance.sh` 는 **`repo/runs` 를 절대 지우지 않는다.** 주석으로 못 박았다.
  처음부터 다시 하려면 `ops/reset.sh` — 거기엔 `repo/.archive/` 백업이 있다.
- `ops/dev.sh status` 가 산출물 개수와 완성된 사이트 개수를 같이 보여준다.
- `ops/dev.sh sites` 로 사이트 주소만 뽑아 볼 수 있다.

### 완성된 사이트 미리보기

`GET /preview/{path}` — repo/ 안의 파일을 올바른 MIME 으로 서빙한다. 읽기 전용이고
repo/ 밖으로는 못 나간다(경로 탈출 테스트 포함).
`GET /preview/sites` 가 `index.html` 이 있는 디렉터리를 전부 찾아 목록으로 준다.
DB 를 보지 않고 파일시스템만 훑으므로 **DB 가 초기화돼도 사이트는 계속 보인다.**

화면 세 군데에 붙였다 — 픽셀 오피스 상단 바 버튼, 서버실 패널 목록, 파일 탐색기의 `열기`.

검증: `core/tests/test_preview.py` 10건 (렌더링·CSS/JS 로드·주문 내용 반영·경로 탈출 차단).

### 최종 인수

```
ops/acceptance.sh   통과 84 · 실패 0   (EXECUTOR=a2a 기준)
```
E2E·편집 테스트는 `sim` 에서만 돈다 — 실제 노드는 한 단계에 수십 초라 대기시간을 넘긴다.
`acceptance.sh` 가 그 사실을 출력하고 건너뛴다.


---

## 결과물 품질 개선 (2026-07-28)

첫 리허설이 만든 사이트를 `ops/site-check.py` 로 채점하니 **19항목 중 10개(53점)**.
viewport 없음, 시맨틱 태그 0개, `<br>` 로 폼 배치, CSS 변수 0개, 한글 폰트 없음,
기본 초록 버튼(#4CAF50) — 요컨대 **설계를 전혀 안 보고 만든 결과물**이었다.

### ★ 진짜 원인은 프롬프트가 아니었다

`pipeline.yaml` 의 `inputs: [SRS.md, ...]` 를 `/api/files?path=SRS.md` 로 그대로
넘기고 있었는데, 실제 파일은 `runs/1/S2/output/SRS.md` 에 있다. **전부 404 였다.**
프론트엔드 에이전트는 화면 목록도 디자인 토큰도 못 본 채 사이트를 만들고 있었다.

`runner.resolve_inputs()` 로 이름 → 실제 경로를 찾아 넘기게 고쳤다 (같은 이름이면 최신).
어댑터는 참고 자료를 하나도 못 받으면 HQ 에 경고를 미러링한다 — 다시 조용히 실패하지 않게.

### 함께 고친 것

- **`pipeline.yaml` 에 `inputs` 를 전 단계에 명시.** 무엇을 읽고 일해야 하는지도
  코드가 아니라 데이터로 남는다.
- **`role_outputs`** — S4 에서 네 역할이 `design-tokens.json`·`schema.sql`·
  `api-contract.yaml` 을 **각자 네 벌씩** 만들고 있었다. 역할별로 나눴다.
- **`templates/_quality.md` 신설.** 모든 작업 지시에 자동으로 붙는다.
  교실 오프라인(CDN·npm 금지), viewport 필수, 시맨틱 태그, 디자인 토큰을 CSS 변수로,
  label/alt/대비 4.5:1, API 공통 응답 형식.
- **`implement.md`** 를 88자 → 2500자 수준으로. 화면 수를 SCREENS.md 에서 세라,
  카드 그리드, localStorage, **톤&매너를 실제 색으로 반영하라(파란 버튼 금지)**.
- **`gate.md`** 에 수용 기준 대조 체크리스트. 파일명과 줄을 짚어 반려하게.
- **`planner_draft_prompt.md`** — AGENT.md 최소 600자, '금지' 칸은 역할마다 다르게.

### 채점 도구

`ops/site-check.py` — 게이트가 보는 것과 같은 19항목을 기계적으로 확인한다.
지시문을 고쳤을 때 결과가 실제로 나아졌는지 숫자로 비교하려고 만들었다.
`ops/acceptance.sh` 가 75점 미만이면 실패로 잡는다.


---

## 품질 개선 결과 — 53점 → 100점 (2026-07-28)

같은 주문(밀밭제과)으로 지시문 고도화 **전후**를 실제로 돌려 비교했다.
`repo/showcase/` 에 둘 다 보존돼 있다.

| | v1 (전) | v2 (후) |
|---|---|---|
| **품질 점수** | **10/19 (53점)** | **19/19 (100점)** |
| viewport | 없음 | 있음 |
| 시맨틱 태그 | 0/5 | 5/5 |
| 레이아웃 | `<br>` 4개 | flex/grid |
| CSS 변수 | 0개 | 24회 사용 |
| 색 | 기본 초록 `#4CAF50` | `#A67C00` 금빛 / `#FFF8E1` 크림 |
| 화면 수 | 1개 (주문폼만) | 5개 (홈·상세·장바구니·로그인·문의) |

디자이너 에이전트가 주문서의 "따뜻하고 아날로그한 느낌" 을 읽고
실제로 베이커리 톤 팔레트를 만들어 냈다. 프론트엔드가 그 토큰을 CSS 변수로 받아 썼다.
**참고 자료를 전달하기 시작하자 이런 것이 가능해졌다.**

### 단계별 소요 (실제 노드, 개선 후)

```
S1 접수  71초    S2 기획 125초  (이전 9분 → 2분)
S3 게이트  5초    S4 설계 5.5분
S5 구현  4분      …
```

S2 가 빨라진 이유: 지시문이 명확해지자 기획이 **한 번에 11개를 다 썼다.**
배치·개별 보충 경로를 타지 않았다. 지시문 품질이 속도까지 바꿨다.

### 그 과정에서 또 잡은 것

- **프론트엔드가 `output/frontend/frontend/` 로 두 겹 만들었다.** 링크가 깨진다.
  → `_quality.md`·`implement.md`·`gate.md` 세 곳에 "output/ 바로 아래 평평하게" 를 넣었다.
- **`design-tokens.css` 를 따로 만들고 `@import` 를 빠뜨렸다.** 색이 하나도 안 먹었다.
  → "토큰은 `style.css` 맨 위 `:root` 에 직접 써라. 파일을 나누지 마라" 를 넣고,
    게이트가 `var(--x)` 정의 여부를 확인하게 했다.

### 채점 도구

`ops/site-check.py` — 19항목. `acceptance.sh` 가 75점 미만이면 실패로 잡는다.
`repo/showcase/README.md` 에 비교표가 있다.


### 마지막에 잡은 것 — 프롬프트가 너무 커졌다

게이트에 심사 파일을 8종 붙이자 프롬프트가 **26KB** 가 됐고, 그 뒤 S5 재작업이
정체했다. 로컬 `gpt-oss:120b` 가 감당하지 못한 것으로 보인다.

→ 어댑터가 참고 자료를 **파일당 4000자 / 전체 14000자**로 자른다.
   잘린 파일은 제목에 `(앞부분만)` 이라고 밝힌다. 조용히 자르면 에이전트가
   "파일이 이게 전부" 라고 오해한다.

**교훈**: 참고 자료를 안 주면 지어내고, 너무 주면 멈춘다.
`inputs` 를 늘릴 때는 프롬프트 크기를 같이 봐야 한다.
노드에서 `~/agora/workspace/cycle-N/<step>/prompt.txt` 크기를 보면 바로 안다.

---

## 구조화 · 사람이 조종하는 시스템으로 (2026-07-29)

PM 지시: **"각 작업별 AGENT.md 를 구체화하고(로컬 모델 성능이 낮으니 Claude Code 로),
역할별 목표를 구체화하고, 전체 구조를 구체화한 뒤 GitHub 에 올려라."**
이어서: **"기능이 겉만 있고 실제로 들어가면 조잡하다. 유저가 AGENT 를 조종할 수 있게 하라."**
최종 목표: **"Claude 를 붙이든 다른 오픈 모델을 붙이든 좋은 품질이 나오게."**

### ★ 이번 구조의 핵심 — 품질을 모델에 맡기지 않는다

```
① 기준선          `agents/<role>/AGENT.md` — 사람(Claude Code)이 쓴 11개
② 참고 자료       inputs → 실제 파일 경로 해석 (안 주면 지어낸다)
③ 완료 조건 주입  roles.yaml 의 DoD 를 매 요청마다 다시 싣는다
④ 기계 검사       checks.py — 형식·존재·수치를 HQ 가 직접 확인
⑤ 자가 재작업     미달이면 실패 목록을 붙여 다시 시킨다 (CHECK_RETRY)
⑥ 게이트          QA·보안·검수가 의미를 본다
```

①~⑤ 는 모델과 무관하다. **모델을 바꿔도 바닥이 유지된다** — 이게 요구의 답이다.

### 내린 결정

1. **지시문 전문을 로컬 모델에 맡기지 않는다.** 기준선은 사람이 쓰고, 기획에게는
   「이번 프로젝트」 네 줄만 맡긴다. `services.compose_spec()` 이 합친다.
   예전 경로(전문 생성)도 그대로 받아 처리하므로 되돌릴 필요가 없다.
2. **역할의 목표·완료조건은 `roles.yaml` 한 파일에.** 코드가 아니라 데이터라서
   강사가 수업 중에 고칠 수 있다. `roles.brief()` 가 프롬프트용으로 1500자 미만으로 압축.
3. **사람의 개입은 파일로 남긴다** (`NOTES.md`). DB 테이블이 아니라 파일인 이유:
   웹에서든 SSH 로든 같은 것을 보고, `git diff` 로 "사람이 무엇을 끼워 넣었나" 가
   그대로 평가 자료가 되고, HQ 재시작을 견딘다.
4. **「지금 고치기」는 예약제.** 한 사이클에 실행기는 하나뿐이라 도는 중에는
   재요청을 받을 수 없다. 그렇다고 "끝나면 다시 누르세요" 라고 할 수는 없으므로
   `.rerun.json` 에 예약하고 실행기가 현재 단계를 마치는 순간 스스로 되감는다.
5. **주문 접수에서 변경/결함/보안 종류를 뺐다.** 아직 아무것도 안 만들었는데
   무엇을 고치라는 말인가. 고치는 요청은 **보면서** 하는 것이라 `/review.html` 로 보냈다.

### 잡은 문제

- **★ YAML 주석 사고.** `- 색이 기본값(#4CAF50, ...)` 에서 ` #` 이 주석으로 먹혀
  완료 조건이 `- 색이 기본값(#4CAF50,` 로 **잘린 채 노드에 전달**되고 있었다.
  아무도 못 알아챘다. → 따옴표로 감싸고, `test_roles.py` 가 문장 끝·괄호 짝을 전수 검사.
- **HQ 재시작 = 사이클 영구 정지.** 실행 태스크는 메모리에 있는데 DB 의 step 은
  RUNNING 으로 남아 화면에는 "진행 중" 인데 아무도 일하지 않았다.
  → `main._recover_interrupted()` 가 부팅 때 되살린다. 실측: 끊긴 S6 을 되살리자
    QA 노드가 즉시 착수했다.
- **「지금 고치기」가 실행 중이면 409 로 거절됐다.** 사람이 버튼을 눌렀는데 아무 일도
  안 일어났다. 이 기능의 존재 이유가 무너진다. → 위 4번.
- **좁은 화면에서 세로 사이드바가 높이 0으로 접혔다.** 편집기 저장 버튼이 화면 밖으로
  나갔다. → 가로 칩 줄로 전환 + 페이지 스크롤 모델로 변경.

### 새로 만든 것

| | |
|---|---|
| `agents/<role>/AGENT.md` | 기준선 11개 (1.6~3.1KB, 서로 다름, git 추적) |
| `core/app/roles.yaml` / `roles.py` | 역할 카탈로그 — 목표·DoD·금지·인계 |
| `core/app/checks.py` | 산출물 기계 검사 (역할별) |
| `core/app/directives.py` | 사람이 끼워 넣는 지시 |
| `core/app/routers/review.py` | 프리뷰 · 지금 고치기 · 작업물 목록 |
| `core/app/routers/activity.py` | 활동 피드 · 받은 지시문 조회 |
| `web/console.html` | 실시간 콘솔 |
| `web/review.html` | 보면서 고치기 |
| `web/projects.html` | 작업별 결과물 |
| `README.md` `ARCHITECTURE.md` `FLOW.md` | 구조·흐름 문서 |
| `core/tests/test_roles.py` | 89건 |

### 검증

```
core/tests/test_roles.py   89건 통과
ops/acceptance.sh          111건 통과 (Phase 6 신설 25건 포함)
실제 노드 라이브 확인       QA 반려 → S5 되감기 → 재작업 티켓 자동 생성
                          → S5 재실행 → dba 완료 조건 6/6 통과
```

GitHub: https://github.com/mimonimo/Web_agency

### 남은 이슈

- **다른 모델 붙이기는 아직 `EXECUTOR` 자리만 있다.** `sim` / `a2a` 두 개뿐이고
  Claude API·OpenAI 호환 백엔드는 안 만들었다. `runner._execute_*` 를 하나 더 만들고
  `EXECUTOR` 에 이름을 더하면 된다 — 지시문·검사·개입은 그대로 쓴다.
- `sudo bash ~/agora-ops/expose-dgx12.sh` (8000·11434 개방, root 필요) 미실행.
- `provisioning/students.yaml` 의 학생 이름이 비어 있다.

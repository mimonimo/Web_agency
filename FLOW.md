# AGORA Web — 수업 흐름

한 사이클이 어떻게 흐르고, **사람이 어디서 무엇을 할 수 있는지**.

---

## 전체 흐름 (신규 개발)

```
        사람                          HQ                        에이전트
  ────────────────           ──────────────────        ────────────────────

  🧾 주문 접수
  /order.html          ──▶   사이클 생성 · S1 시작   ──▶  S1 영업
                                                          범위 합의 (SOW·견적)
                             ✅ 완료 조건 검사
                                                     ──▶  S2 기획
                                                          SRS · 화면 목록
                                                          11명의 「이번 프로젝트」
                             기준선 + 이번 프로젝트
                             = AGENT.md 11개 생성

  👥 학생 11명            ◀──  S3 여기서 멈춘다
  /edit.html                   (BLOCKED)
  자기 AGENT.md 보강
  저장 ──────────────▶   11/11 되면 자동 재개
                                                     ──▶  S4 설계 (4명 동시)
                                                          디자인 토큰 · 스키마
                                                          API 계약 · 실행 환경
  🖥 실시간 콘솔                ✅ 검사 → 미달이면 재작업
  /console.html
  지금 뭐 하는지 본다                                  ──▶  S5 구현 (3명 동시)
                                                          화면 · 서버 · 시드
  👀 보고 고치기          ◀──  프리뷰가 생긴다
  /review.html
  "버튼이 너무 커요"  ──▶   지시를 꽂고 그 단계 재실행 ──▶  다시 만든다 (2~5분)

                                                     ──▶  S6 QA 게이트
                                                     ──▶  S7 보안 게이트
                                                     ──▶  S8 고객 검수
  🎫 티켓 보드            ◀──  반려면 되감기 + 티켓
  /board.html                                        ──▶  S9 배포
  통과/반려를 직접                                     ──▶  S10 운영 관찰
                                                          → 다음 주문 발행
  📦 작업물
  /projects.html        ◀──  DONE
  결과물을 본다
```

**소요 시간**: 한 사이클 25~40분. S2 기획이 가장 오래 걸린다.

---

## 사람이 개입하는 다섯 지점

### 1. 주문 — `/order.html`

새 일을 맡긴다. **여기는 신규 전용이다.**
이미 만들어진 것을 고치는 요청은 `/review.html` 로 간다 —
만들어진 것을 **보면서** 말해야 정확하기 때문이다.

고른 기능 개수만큼 화면이 늘어난다. 욕심내면 미완성으로 끝난다.
「톤 & 매너」에 적은 문장을 디자이너가 실제 색값으로 옮긴다.

### 2. S3 게이트 — `/edit.html` ★ 수업의 핵심

11명이 각자 자기 AGENT.md 를 고친다. **11명이 다 저장해야 사이클이 재개된다.**

| 탭 | 무엇 |
|---|---|
| ✏️ 이번 지시문 | 고칠 수 있는 파일. 저장하면 카운터가 오른다 |
| 📘 기준선 | 원래 뭐라고 적혀 있었나 (읽기 전용) |
| ✍️ 추가 지시 | 작업 도중 끼워 넣을 말 |
| 🔍 초안과 비교 | 내가 무엇을 바꿨나 — **회고 자료** |

각 파일의 경로가 화면에 그대로 보이고, 복사 버튼이 있다.

### 3. 작업 도중 — `/agent.html`, `/review.html`

**"디자인 파트에 지시를 하나 더 주고 싶다"** 를 실제로 할 수 있다.

```
/agent.html?role=designer  →  「이 역할에게 지시 추가」에 한 줄
                           →  repo/<project>/agents/designer/NOTES.md 에 저장
                           →  디자이너가 다음에 일할 때
                              프롬프트 맨 뒤에 "★ 최우선" 으로 붙는다
```

지금 돌고 있는 단계에 반영하려면 같은 화면의 **↻재요청**을 함께 누른다
(사이클이 돌고 있으면 ⏸일시정지 먼저).

`/review.html` 은 이걸 한 번에 한다 — 프리뷰를 보면서 한 줄 적고
**「지금 고치기」**를 누르면 지시 꽂기 → 일시정지 → 재실행이 자동으로 이어진다.

### 4. 게이트 판정 — `/board.html`

QA·보안·검수 에이전트가 `VERDICT.md` 첫 줄에 `판정: 통과|반려` 를 쓰면
HQ 가 읽어 자동 처리한다 (`GATE_AUTO=0` 으로 끄면 수동).

사람이 직접 누를 수도 있다.
- **통과** — 사유를 남긴다. 왜 통과시켰는지 없으면 나중에 아무도 설명 못 한다.
- **반려** — 사유가 그대로 재작업 티켓이 된다. 지정한 단계로 되감는다.

### 5. 아무 때나 — `/index.html`

⏸일시정지 · ▶재개 · ⏭한 단계 · ⏮되감기 · ⟲처음부터.

> **⟲ 처음부터** 는 기본이 **「AGENT.md 는 그대로 두기」** 다.
> 같은 주문을 지시문만 바꿔 다시 돌려 보는 것이 이 수업의 핵심이라서다.

---

## 결과물 품질이 유지되는 방식

모델이 좋아서가 아니라 **구조가 잡아 준다.**

```
① 기준선          사람이 쓴 역할 지시문 — 6칸, 완료조건, 역할별 금지
② 참고 자료       inputs 를 실제 파일 경로로 해석해 전달 (안 주면 지어낸다)
③ 완료 조건 주입  roles.yaml 의 DoD 를 매 요청마다 다시 싣는다
④ 기계 검사       내고 나면 HQ 가 확인한다 (형식·존재·수치)
⑤ 자가 재작업     미달이면 실패 목록을 붙여 다시 시킨다
⑥ 게이트          QA·보안·검수가 의미를 본다 → 반려면 되감기
```

①~⑤ 는 모델과 무관하다. 그래서 **모델을 바꿔도 바닥이 유지된다.**

실제 측정 (같은 주문, 같은 모델):

| | 지시문 개선 전 | 개선 후 |
|---|---|---|
| 사이트 품질 | 10/19 (53점) | 19/19 (100점) |
| viewport | 없음 | 있음 |
| 시맨틱 태그 | 0/5 | 5/5 |
| CSS 변수 | 0개 | 24회 사용 |
| 색 | 기본 초록 `#4CAF50` | 밀밭제과 톤 `#A67C00` |
| 화면 | 1개 | 5개 |

둘 다 `repo/showcase/` 에 남아 있다 — `/projects.html` 에서 비교할 수 있다.

---

## 회고 (수업 마지막 15분)

1. `/projects.html` 에서 완성된 사이트를 연다.
2. `/edit.html` → 🔍 초안과 비교 — **내가 무엇을 바꿨나.**
3. `/console.html` 에서 에이전트를 눌러 **실제로 받은 지시문**을 본다.
4. `⟲ 처음부터` → 「AGENT.md 는 그대로 두기」 로 같은 주문을 다시 돌린다.

> **지시문 한 줄이 결과를 어떻게 바꿨나** — 배울 것은 이것 하나다.

---

## 자주 막히는 곳

| 증상 | 원인 | 조치 |
|---|---|---|
| 책상이 다 꺼져 있다 | 하트비트 30초 주기 | DB 를 새로 만들었으면 1분 기다린다 |
| 단계가 안 끝난다 | 노드 무응답 | `/console.html` 경과 시간 확인 → ↻재요청 |
| 게이트에서 계속 반려 | 요구사항 자체가 문제 | 두 번 반려되면 기획(S2)으로 되감는다 |
| 사이트가 안 열린다 | 산출물 경로 | `/projects.html` 또는 `recover-artifacts.sh` |
| HQ 를 재시작했다 | 끊긴 단계 | 부팅 때 자동 복구된다 (로그에 남는다) |
| 결과물이 부실하다 | 참고 자료 미전달 | `/console.html` 에서 지시문을 열어 `참고자료 N건` 확인 |

결함 신고를 읽고 **재현 절차**를 만든다. `REPRO.md` 에 쓴다.

```markdown
# 재현 절차

## 환경
- 어느 파일 / 어느 화면

## 절차
1. …을 연다
2. …에 …을 입력한다
3. …버튼을 누른다

## 기대한 결과
…

## 실제 결과
… (오류 메시지가 있으면 그대로 옮긴다)

## 원인 추정
어느 파일 어느 부분이 의심스러운가. 코드를 인용한다.
```

재현되지 않으면 **"재현 불가"** 라고 쓰고, 무엇을 더 물어봐야 하는지 적는다.
추측으로 "고쳤을 것" 이라고 쓰지 마라.

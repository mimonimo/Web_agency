#!/usr/bin/env bash
# 되감기 CLI (BRIEF §2) — Phase 3c 에서 구현한다.
#
#   ops/rewind.sh <cycle_id> <step_key>
#
# 그 step 이후의 모든 산출물을 무효화하고 거기서 재개한다 (인수 #20).
# 픽셀 오피스의 ⏮ 버튼과 같은 일을 하는 강사용 터미널 버전이다.
set -euo pipefail
echo "Phase 3c 에서 구현한다." >&2
exit 1
